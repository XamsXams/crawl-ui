const routes = {
    home: '/',
    login: '/login',
    register: '/register',
    post: '/post/:slug',
    write: '/write',
    profile: '/profile',
    category: '/:slug',
};

export default routes;
